[pytest]
markers =
    slow: needs live infrastructure (a reachable AgentENV, Docker, credentials).
        Such tests skip themselves when it is absent, so a plain run is
        unaffected; `-m "not slow"` excludes them outright.
