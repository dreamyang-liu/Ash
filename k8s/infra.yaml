apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane
  namespace: apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: control-plane
  template:
    metadata:
      labels:
        app: control-plane
    spec:
      serviceAccountName: control-plane
      containers:
      - name: control-plane
        image: rl-sandbox-cp:0.1
        imagePullPolicy: IfNotPresent
        env:
        - name: TARGET_NAMESPACE
          value: "apps"
        - name: REDIS_HOST
          value: "redis.apps.svc.cluster.local"
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_DB
          value: "0"
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "300m"
            memory: "256Mi"
          limits:
            cpu: "800m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: apps
spec:
  type: NodePort
  selector:
    app: control-plane
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
      - name: gateway
        image: rl-sandbox-gateway:0.1
        imagePullPolicy: IfNotPresent
        env:
        - name: REDIS_ADDR
          value: "redis.apps.svc.cluster.local:6379"
        - name: REDIS_DB
          value: "0"
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "300m"
            memory: "256Mi"
          limits:
            cpu: "800m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /healthz
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
      # Wait for redis to be ready before starting gateway
      initContainers:
      - name: wait-for-redis
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z redis.apps.svc.cluster.local 6379; do echo waiting for redis; sleep 2; done;']
        resources:
          requests:
            cpu: "50m"
            memory: "32Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: apps
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: apps
  labels: { app: redis }
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: apps
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: apps
  labels: { app: redis }
spec:
  replicas: 1
  selector:
    matchLabels: { app: redis }
  template:
    metadata:
      labels: { app: redis }
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        imagePullPolicy: IfNotPresent
        args: [
          "redis-server",
          "--appendonly", "yes",
          "--save", "900", "1",
          "--protected-mode", "no",
          "--maxmemory", "400mb",
          "--maxmemory-policy", "allkeys-lru",
          "--tcp-keepalive", "60"
        ]
        ports:
        - containerPort: 6379
        resources:
          requests: { cpu: "150m", memory: "256Mi" }
          limits:   { cpu: "500m", memory: "512Mi" }
        livenessProbe:
          tcpSocket: { port: 6379 }
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          tcpSocket: { port: 6379 }
          initialDelaySeconds: 2
          periodSeconds: 5
        volumeMounts:
        - name: redis-data
          mountPath: /data
      volumes:
      - name: redis-data
        persistentVolumeClaim:
          claimName: redis-data
